import type { CalculatorResult, RemoteWorkExpenseInputs } from "@/lib/calculators/types";
export function calculateRemoteWorkExpenses(input: RemoteWorkExpenseInputs): CalculatorResult { const monthly = input.internet + input.utilities + input.software; const annualRecurring = monthly * 12; const annual = annualRecurring + input.workspace; return { primary: annual, kind: "currency", details: [{ label: "Recurring monthly cost", value: monthly, kind: "currency" }, { label: "Recurring annual cost", value: annualRecurring, kind: "currency" }, { label: "Average per calendar month", value: annual / 12, kind: "currency" }] }; }
