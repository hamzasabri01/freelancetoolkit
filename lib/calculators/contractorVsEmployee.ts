import type { CalculatorResult, ContractorVsEmployeeInputs } from "@/lib/calculators/types";
export function calculateContractorVsEmployee(input: ContractorVsEmployeeInputs): CalculatorResult { const contractorRevenue = input.contractRate * input.contractHours; const contractorValue = contractorRevenue - input.contractExpenses; const employeePackage = input.salary + input.benefits; return { primary: contractorValue - employeePackage, kind: "currency", details: [{ label: "Contractor revenue", value: contractorRevenue, kind: "currency" }, { label: "Contractor value after expenses", value: contractorValue, kind: "currency" }, { label: "Employee total package", value: employeePackage, kind: "currency" }, { label: "Break-even contract rate", value: (employeePackage + input.contractExpenses) / input.contractHours, kind: "currency" }] }; }
